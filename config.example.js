// Template configuration file
// Copy this to public/config.js and set your GTM/GA4 ID
const GTM_ID = 'GTM-XXXXXXX'; // or 'G-XXXXXXXXXX'
if (typeof window !== 'undefined') {
  window.GTM_ID = GTM_ID;
}
